# BMS 测试用例生成平台 — 后端依赖（Linux）
# Python 3.11+

# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# 数据库
SQLAlchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.2.5

# 缓存/队列
redis>=5.0

# 调度（过期清理）
APScheduler>=3.10

# PDF / CSV 解析
pypdf>=4.2
PyMuPDF>=1.24
pytesseract>=0.3.10
pillow>=10.0

# 本地模型（OpenAI 兼容）
openai>=1.30

# 组合测试（pairwise）
allpairspy>=2.5

# 工具
python-dotenv>=1.0
httpx>=0.27

# 导出 xlsx/docx 用(可选)：仅当需要 Excel/Word 导出时安装。
# HTML 导出零依赖、始终可用;未装下面两个包时 xlsx/docx 接口返回友好提示。
# pip install openpyxl python-docx
# openpyxl>=3.1
# python-docx>=1.1
